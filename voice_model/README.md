# Voice Model（声控模型）

放置声控模型相关配置与推理代码。

## 建议内容

- 标签映射、采样率、特征说明
- 推理入口脚本
- 与 Arduino / 上位机联调方式

## 模型文件

较小的模型 / 配置可以直接放进本目录并提交。  
若单文件过大（约 50–100MB 以上）推送失败，可改用 Git LFS，或外链下载。

**大文件下载链接（如有）：** （待补充）
